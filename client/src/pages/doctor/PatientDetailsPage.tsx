import PhasePlaceholder from "../../components/PhasePlaceholder";

/** PatientDetailsPage — see spec section 14. Built in Phase 3. */
export default function PatientDetailsPage() {
  return (
    <PhasePlaceholder
      title="Patient Details"
      description="Basic information for an authorized patient."
      phase="Phase 3"
    />
  );
}