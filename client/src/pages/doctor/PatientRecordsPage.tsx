import PhasePlaceholder from "../../components/PhasePlaceholder";

/** PatientRecordsPage — see spec section 14. Built in Phase 3. */
export default function PatientRecordsPage() {
  return (
    <PhasePlaceholder
      title="Patient Records"
      description="Record list and timeline for an authorized patient."
      phase="Phase 3"
    />
  );
}