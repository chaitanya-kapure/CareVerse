import PhasePlaceholder from "../../components/PhasePlaceholder";

/** UploadRecordPage — see spec section 14. Built in Phase 2. */
export default function UploadRecordPage() {
  return (
    <PhasePlaceholder
      title="Upload Record"
      description="Add a PDF report, prescription or discharge summary."
      phase="Phase 2"
    />
  );
}