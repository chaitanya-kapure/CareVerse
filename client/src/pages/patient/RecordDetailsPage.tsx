import PhasePlaceholder from "../../components/PhasePlaceholder";

/** RecordDetailsPage — see spec section 14. Built in Phase 2. */
export default function RecordDetailsPage() {
  return (
    <PhasePlaceholder
      title="Record Details"
      description="Extracted text and structured fields for one document."
      phase="Phase 2"
    />
  );
}