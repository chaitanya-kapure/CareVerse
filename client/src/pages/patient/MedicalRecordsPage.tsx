import PhasePlaceholder from "../../components/PhasePlaceholder";

/** MedicalRecordsPage — see spec section 14. Built in Phase 2. */
export default function MedicalRecordsPage() {
  return (
    <PhasePlaceholder
      title="Medical Records"
      description="Every PDF you have uploaded, newest first."
      phase="Phase 2"
    />
  );
}